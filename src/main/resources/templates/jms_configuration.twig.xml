<?xml version="1.0" encoding="UTF-8" ?>
<serializer>
    <class name="{{ class }}" exclusion-policy="ALL">
        {%- for property, type in properties %}
        <property name="{{ property }}" type="{{ type }}" expose="true" />
        {%- endfor %}
    </class>
</serializer>
